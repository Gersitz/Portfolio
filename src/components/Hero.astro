---
import { Image } from "astro:assets";
import me from "../assets/images/me.png";
---

<section class="hero">
    <div class="hero-text">
        <p class="greeting">Greet</p>
        <h2>Name</h2>
        <h3>Title</h3>
        <p>
            Lorem ipsum dolor sit, amet consectetur adipisicing elit. Iste voluptatum consequuntur ipsam molestias cum provident illo eaque nihil saepe consequatur dolorem expedita ducimus aliquid incidunt ratione aspernatur officia culpa earum assumenda, pariatur placeat? Architecto obcaecati distinctio possimus enim cumque, est autem deleniti cum incidunt, esse quae repudiandae laborum consequatur adipisci.
        </p>
    </div>
    <div class="hero-image">
        <Image src={me} alt="text" id={"me-image"} />
    </div>
</section>

<style>
    section.hero {
        margin-bottom: 4rem;
        display: flex;
        flex-direction: row;
        overflow-y: hidden;
        align-items: center;
    }

    section.hero > div.hero-text {
        display: flex;
        flex-direction: column;
        justify-content: center;
        width: 70vw;
        margin: 0 5vw 0 auto;
        position: relative;
        z-index: -2;
    }
    section.hero > div.hero-image {
        width: 30vw;
        position: relative;
        z-index: 10;
    }
    .hero-text > * {
        text-align: center;
        width: 100%;
    }
    .hero-text > :not(:last-child) {
        line-height: 100%;
    }
    .hero-text > p.greeting {
        font-size: 1.3em;
        color: rgb(255, 110, 195);
        margin: 0;
    }
    .hero-text > h2 {
        margin-top: .5rem;
        font-size: 3em;
        font-weight: 800;
        margin: .5rem 0 1.4rem 0;
    }
    .hero-text > h3 {
        color: rgb(200 140 255);
        font-size: 1.4em;
        font-weight: 600;
        margin-top: 1rem;
        margin: 0 0 1rem 0;
    }
    .hero-text p {
        /* max-width: 45ch; */
        font-size: 1.1em;
    }
    .hero-image > #me-image {
        object-fit: cover;
        height: auto;
        border-radius: 2rem;
    }
    @media only screen and (max-width: 900px) {
        section.hero {
            flex-direction: column;
            font-size: .9em;
            height: fit-content;
            align-items: center;
        }
        section.hero > div.hero-text {
            max-width: none;
            margin: 0 1rem;
            position: static;
        }
        section.hero > div.hero-image {
            width: 100%;
            position: static;
            align-items: center;
            display: flex;
            justify-content: center;
        }
        .hero-text p {
            width: 100%;
            max-width: none;
        }
    }
</style>
